name: Mantener Aplicación Activa

on:
  schedule:
    # Este cron se ejecuta cada 20 minutos. 
    # Vercel suele suspender apps tras periodos de inactividad.
    - cron: '*/20 * * * *'
  workflow_dispatch: # Permite ejecutarlo manualmente desde GitHub

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    steps:
      - name: Hacer ping a la aplicación
        run: |
          # Cambia la URL por la de tu aplicación real
          curl -I https://agendaeliaquin.vercel.app/